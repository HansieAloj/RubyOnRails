<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand", id: "logo" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Articles", articles_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Bloggers", users_path, class: "nav-link" %>
      </li>


      <li class="nav-item">
          <% if logged_in? %>
            <%= link_to "Create Article", new_article_path, class: "nav-link" %>
          <% end %>  
      </li>

      <li class="nav-item">
          <% if logged_in? %>
            <%= link_to "Delete Profile", user_path(current_user), data: {turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this Account?'}, class: "nav-link" %>
          <% end %>  
      </li>


      <% if logged_in? %>
        <li class="nav-link">
            <%= "(Admin)" if current_user.admin? %> Profile [<%= current_user.username %>]
        </li>
        <li class="nav-item">
        <%= link_to 'Logout', logout_path, class: "nav-link", data: {turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to 'Log in', login_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Sign up', signup_path, class: "nav-link" %>
        </li>
      <% end %>

      <% if logged_in? && current_user.admin? %>
        <li class="nav-item">
          <%= link_to "Create new category", new_category_path, class: "nav-link" %>
        </li>
      <% end %>  
      <li class="nav-item">
        <%= link_to "View categories", categories_path, class: "nav-link" %>
      </li>

    </ul>
    <%# <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form> %>
  </div>
</nav>